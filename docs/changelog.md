# Changelog

## [Unreleased]

- 2026-07-31 — [#1](https://github.com/rustycoopes/top-trumps-game/issues/1) Slice 0 — NSD and platform spike: confirmed no runtime permission prompt for NSD discovery on Android 13/14, no `MulticastLock` needed, auto-rename format (`"<name> (2)"`), and `resolveService` concurrency behaviour. Local Network Protection (API 36) remains untested pending hardware. See [slice-0-nsd-platform-spike.md § Delivered](features/top-trumps-core-game/WBS/slice-0-nsd-platform-spike.md#delivered).
- 2026-07-31 — [#2](https://github.com/rustycoopes/top-trumps-game/issues/2) Slice 1 — Walking skeleton: stood up the six-module structure, structurally-redacted `PlayerView`/`OpponentCardView` types, a synchronous `RulesEngine`, deck manifest loading through `DeckSource`, a `MatchSession` (host + guest) over `LoopbackTransport` round-tripping a real JSON codec, a trivial AI opponent, and an ugly Compose screen — one round of Top Trumps played solo, end to end, verified on a physical device. See [slice-1-walking-skeleton.md § Delivered](features/top-trumps-core-game/WBS/slice-1-walking-skeleton.md#delivered).
- 2026-07-31 — [#3](https://github.com/rustycoopes/top-trumps-game/issues/3) Slice 2 — Complete solo match: the full fifteen-round loop with strict turn alternation, interactive tiebreak recursion, the `CHOOSER_WINS`/`DEFENDER_WINS`/`EACH_KEEPS_OWN` all-metrics-tie fallback, win piles, a live score/round counter, a browsable win-pile UI that preserves round state, a victory/defeat result screen with rematch, and a deck-relative AI opponent — verified end to end on a physical device. See [slice-2-complete-solo-match.md § Delivered](features/top-trumps-core-game/WBS/slice-2-complete-solo-match.md#delivered).
- 2026-07-31 — [#4](https://github.com/rustycoopes/top-trumps-game/issues/4) Slice 3 — The real Motorcycles deck: thirty real, sourced-and-cited motorcycles (1923–2018) with Wikimedia Commons CC-licensed photographs, a documented weight/top-speed/length convention for cards where published figures disagree or don't exist, tightened deck manifest validation, a deck picker, and Coil-based card/win-pile image rendering with an "Age — N years" display and win-direction indicators — verified end to end on a physical device. See [slice-3-motorcycles-deck.md § Delivered](features/top-trumps-core-game/WBS/slice-3-motorcycles-deck.md#delivered).
- 2026-07-31 — [#5](https://github.com/rustycoopes/top-trumps-game/issues/5) Slice 4 — Discovery, lobby and invitations: zero-resolve mDNS peer discovery with TTL-pruned self-filtering, a pure tap-to-invite/accept/decline state machine with a receive-side mutual-invitation UUID tiebreak and `BUSY` handling, a real length-prefixed `TcpTransport` over Wi-Fi-network-bound socket factories, an `NsdManager`/`ConnectivityManager` wrapper in `:platform:net`, and Compose UI for name entry, lobby, settings and a manual connect-by-address fallback — no game yet, the success condition is a connected socket and a screen that says so. Single-device on-hardware smoke-tested (name entry, empty-lobby hint, manual-connect address display, no runtime permission prompt, no regression to solo mode); two-device scenarios (discovery, invite/accept, the tiebreak, `BUSY`, TTL removal, no-internet Wi-Fi) remain to be verified on a second phone. See [slice-4-discovery-lobby.md § Delivered](features/top-trumps-core-game/WBS/slice-4-discovery-lobby.md#delivered).
- 2026-08-01 — [#6](https://github.com/rustycoopes/top-trumps-game/issues/6) Slice 5 — Two-device match: the real `GuestToHost`/`HostToGuest` wire protocol (guest thin-client property enforced by the type system), the protocol-version and deck-hash handshake gates, `HostMatchSession`/`GuestMatchSession` sending a real dealt hand and view stream over the same path solo mode already uses, and a `MatchController`/`TwoDeviceMatchScreen` driving the whole flow in `:app`. Fixed a critical `LobbyController`/`MatchController` transport-listener race and two crash-on-disconnect/malformed-frame bugs found in code review. Resolved the rematch-host-continuity open question (host stays host); two-device rematch itself is deferred to a follow-up issue. No physical device was available to verify the on-device acceptance criteria this session. See [slice-5-two-device-match.md § Delivered](features/top-trumps-core-game/WBS/slice-5-two-device-match.md#delivered).
- 2026-08-01 — [#7](https://github.com/rustycoopes/top-trumps-game/issues/7) Slice 6 — Resilience: a `connectedDevice` foreground service for match duration, a 2s/3-miss (~6s) application heartbeat, a 60s reconnect countdown, full-state resync with sequence-number dedup so a choice in flight during a drop is applied exactly once, direct-redial-then-NSD-fallback reconnection with no new wire field, deliberate-quit-vs-crash distinction, and keep-screen-on scoped to the match screen. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a critical bug where an Activity recreation mid-match silently killed it (no orientation lock anywhere), plus transport/coroutine leaks on reconnect, a courtesy-message flush race, a cross-thread data race, and a double-`leave()` crash. No physical device was available to verify the on-device acceptance criteria this session. See [slice-6-resilience.md § Delivered](features/top-trumps-core-game/WBS/slice-6-resilience.md#delivered).
- 2026-08-01 — [#8](https://github.com/rustycoopes/top-trumps-game/issues/8) Slice 7 — Polish: a Compose `stabilityConfigurationFile` for `:core:rules`/`:core:session` (verified via before/after compiler metrics — every match-screen composable taking a session/view type flipped `unstable` → `stable`), a card-flip reveal with correct perspective and no mirrored back face, a slide-to-pile animation, a two-signal `AnimationGate` that hard-cuts on a two-device resync or after backgrounding without misfiring on live play, six SoundPool-based sound effects (placeholder audio) with a persisted mute toggle, and edge-to-edge inset handling centralized on the `NavHost`. Also finally wired up slice 6's `resync` wire flag via a new `MatchSession.lastResync`. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a critical DataStore collision that would have crashed every launch, a lifecycle-latch bug that could permanently suppress live animations, two sequential-instead-of-concurrent/leaked slide-animation bugs, and a per-frame recomposition mistake in the slide overlay. No physical device was available to verify the on-device acceptance criteria this session. See [slice-7-polish.md § Delivered](features/top-trumps-core-game/WBS/slice-7-polish.md#delivered).
- 2026-08-01 — [#9](https://github.com/rustycoopes/top-trumps-game/issues/9) Slice 8 — Match history: a new `:feature:history` Room module (schema exported and committed) recording completed matches and deriving history/head-to-head/overall-record/most-won-cards views, fed by a `MatchSession.completedMatch` collector in `:core:session` that structurally cannot fire on a quit or abandonment, decorated with an injected clock and opponent name, and consumed by `:app`'s `AppGraph`/`MatchController` for both solo and two-device matches — with a new architectural test extending `checkCoreDependencyAllowlist` to enforce `:core:*` never depends on `:feature:history`. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a semantic bug where "cards won" credited a seat's whole final pile (including cards captured off the opponent) rather than the cards it actually won a round with, an unbounded/unscrollable stats screen, a leaked collector on an abandoned match, an unguarded write that could crash the app, and a `:feature:history`/`:core:rules` naming collision; one pre-existing manual-connect defect (records the peer's IP as their name) was filed as [#21](https://github.com/rustycoopes/top-trumps-game/issues/21) rather than fixed here. No physical device was available to verify the two new screens on-device this session. See [slice-8-match-history.md § Delivered](features/top-trumps-core-game/WBS/slice-8-match-history.md#delivered).
- 2026-08-01 — [#11](https://github.com/rustycoopes/top-trumps-game/issues/11) Consolidate duplicate metric-choosing test helpers in `MatchSessionTest`: migrated its remaining `chooseMetric(host, guest, metric)` call sites onto the shared `MatchDriver` test helper and deleted the now-dead standalone function, so the file uses one sequencing mechanism throughout as the Slice 2 WBS originally asked for. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found no issues in the change itself but noted a third copy of the same dispatch logic had since arrived with Slice 6's `ConnectionResilienceTest`, out of this issue's scope — filed as [#23](https://github.com/rustycoopes/top-trumps-game/issues/23). See [slice-2-complete-solo-match.md § Delivered](features/top-trumps-core-game/WBS/slice-2-complete-solo-match.md#delivered).
- 2026-08-01 — [#23](https://github.com/rustycoopes/top-trumps-game/issues/23) Consolidate the same duplicate helper in `ConnectionResilienceTest`: migrated its five `resolveOneRound(host, guest)` call sites onto `MatchDriver` and deleted the standalone function, matching #11's fix in the sibling file. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) confirmed the change is behaviourally identical (the fixture deck is single-metric, so hardcoding the known metric is equivalent to the deleted helper's "first metric in the view" pick) and found no further duplicates of this shape in `:core:*`. See [slice-2-complete-solo-match.md § Delivered](features/top-trumps-core-game/WBS/slice-2-complete-solo-match.md#delivered).
- 2026-08-02 — [#26](https://github.com/rustycoopes/top-trumps-game/issues/26) Slice 1 — Test infrastructure spike (card-visual-identity): wired up JUnit 4 + Robolectric + Compose UI testing in `:app`, confirming AGP 9.1.1/Gradle 9.3.1 support Robolectric — the ADR's "one genuine unknown" — via a real JDK 17 run of a single throwaway smoke test. `robolectric.properties` also isolates every `:app` unit test from real app startup (`TopTrumpsApplication`'s NSD/Room/sound setup). Code review found local Robolectric runs fail under the Android Studio JBR (JDK 25) for an unrelated, pre-existing reason unaffected by CI; filed as [#31](https://github.com/rustycoopes/top-trumps-game/issues/31). See [slice-1-test-infrastructure-spike.md § Delivered](features/card-visual-identity/WBS/slice-1-test-infrastructure-spike.md#delivered).
- 2026-08-02 — [#27](https://github.com/rustycoopes/top-trumps-game/issues/27) Slice 2 — Theme, deck data & graph plumbing foundation (card-visual-identity): an optional `theme` block in deck manifests parsed into a `DeckTheme`/`ArgbColor` value class in `:core:rules`, with the load-bearing validation-severity split (malformed accent/`onAccent`/`heroCardId` degrades to the default at runtime, but fails a new all-decks CI test) — `decks/motorcycles` and `decks/lucys-youtubers` both now carry a real theme block. The app's first `MaterialTheme`, a bundled Anton display font, and a `values`/`values-night` themes.xml pair fixing a cold-start dark-mode flash. `AppGraph` gained a memoised `deckTheme(deckId)` and now owns the app's single shared `ImageLoader` (moved out of `MatchScreen`). Pure `cardGeometry`/`solveCardWidth` functions implementing the card-geometry ADR's grow-then-absorb sizing maths, tested with no Robolectric needed. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a broken cache-memoisation bug (`getOrPut` on a nullable-valued map never actually cached a miss) and an unsynchronized cross-thread mutation of the same cache — both fixed together with a `ConcurrentHashMap`. The cold-start flash fix and font rendering were not verified on a physical device this session. See [slice-2-theme-deck-data-foundation.md § Delivered](features/card-visual-identity/WBS/slice-2-theme-deck-data-foundation.md#delivered).
- 2026-08-02 — [#28](https://github.com/rustycoopes/top-trumps-game/issues/28) Slice 3 — The card composable (card-visual-identity): a two-layer `TrumpCard`/`TrumpCardBack` (pure, no Coil/assets, every colour from an explicit `CardPalette`) plus a thin `AssetTrumpCard` wrapper owning the only `file:///android_asset/...` reference and the Coil decode-size guarantee, per the card-image-slot ADR — full and mini variants, a five-row stat table with win-direction arrows and loud/quiet reveal marking, a font-scale cap at 1.3×, and a pure `cardContentOf` mapper taking an injectable `Clock`. A debug-build-only card gallery, reachable from Settings and gated at the nav-graph level, renders every card of a real deck at real size from real assets. This is the repo's first Compose UI test: four Robolectric semantics tests cover the interaction contract (clickable/disabled rows, correct callback and metric key, mini's zero tap targets, win/lose/tie `stateDescription`). Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a real bug where the mini variant's Coil decode-size request didn't match its actual rendered size (the stat table's height is reallocated into the image window for mini, but the decode-size calculation hadn't followed) — fixed with a single `CardGeometry.imageZoneHeight` source of truth. Two non-blocking findings were filed rather than fixed in-slice: [#34](https://github.com/rustycoopes/top-trumps-game/issues/34) (a duplicated wire-type mapper that could be shared from `:core:session`) and [#35](https://github.com/rustycoopes/top-trumps-game/issues/35) (a stale WBS claim about `Modifier.clickable(enabled = false)`'s semantics, corrected in the shipped code but not yet in the doc). `MatchScreen.kt` is deliberately untouched — out of this slice's scope. No physical device was available to verify the on-device-only acceptance criteria this session. See [slice-3-card-composable.md § Delivered](features/card-visual-identity/WBS/slice-3-card-composable.md#delivered).
- 2026-08-02 — [#29](https://github.com/rustycoopes/top-trumps-game/issues/29) Slice 4 — Match screen redesign (card-visual-identity): restructured the match screen from a scrolling column into three fixed, non-scrolling regions — a top bar, a `Modifier.weight(1f)` card region, and a bottom action strip — with the five stat-choosing `Button`s replaced by the hero card's own tappable stat rows. On reveal, the opponent's card flips in beside the player's own, both built from one shared `CardGeometry` instance so row alignment is structural rather than measured. Reworked `CardAnimations.kt`'s flip/slide to take content lambdas instead of a fixed card shape, fixing the documented per-frame recomposition bug (an `Animatable.value` read in a composable body) by composing both flip faces once and driving visibility from inside `graphicsLayer {}` — confirmed via a fresh Compose compiler metrics run. First merged PR #36 (Slice 3's card composable, closed-but-unmerged when this slice started) so this slice could build on the real shipped composable. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a real bug — `FlippableOpponentCard`'s always-composed faces left the settled-away back face permanently in the accessibility tree — plus a missing dim cue on the hero card during the opponent's turn and two parameter-style inconsistencies; nothing rose to a follow-up-issue bar. No physical device was available to verify the on-device-only acceptance criteria (flip perspective, slide shape, recomposition/frame counts, inset clearance) this session. See [slice-4-match-screen-redesign.md § Delivered](features/card-visual-identity/WBS/slice-4-match-screen-redesign.md#delivered).
- 2026-08-02 — [#30](https://github.com/rustycoopes/top-trumps-game/issues/30) Slice 5 — Remaining surfaces & deck accents (card-visual-identity): the win-pile grid now renders mini cards instead of bare thumbnails, and tapping one opens it full-size and read-only, closing PRD story 49; the deck picker is now a grid of themed mini cards (accent colour + representative image) instead of plain buttons, backed by a new `DeckSummary.heroImageFile` resolved once in `AppGraph.listDecks()`; the result screen now names `MaterialTheme` typography/colours explicitly instead of only inheriting them implicitly; and `lucys-youtubers`' accent colour moved from `#FF0000` (≈4.0:1 contrast, failing the PRD's ≥4.5:1 bar) to a hot-pink `#C2185B` (≈5.87:1) — Motorcycles' existing accent already passed and was left unchanged. No second `ImageLoader` instance was introduced anywhere in this slice. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a real bug where the new tap targets' `.clickable()` sat outside the card's own internal `clip` in the composed modifier chain, letting the tap ripple bleed past the card's rounded corners; three minor nits (a stale doc comment, a missing `fillMaxSize()`, an inconsistent `remember` key) were also fixed. The acceptance criterion needing two physical devices (a `theme`-bearing manifest doesn't disturb the handshake) was **not** verified this session, none was available. See [slice-5-remaining-surfaces.md § Delivered](features/card-visual-identity/WBS/slice-5-remaining-surfaces.md#delivered).
- 2026-08-06 — [#40](https://github.com/rustycoopes/top-trumps-game/issues/40) Slice 2 — Lobby theme (second-round-updates): reskinned `LobbyScreen.kt` onto a new "The Pack" warm-tabletop `LobbyPalette` — reused from `card-visual-identity`'s `DeckTheme.DEFAULT` accent rather than a new colour — after mocking up and signing off on one of three visual directions first. Card-shaped peer rows with a hard-offset drop shadow, a dashed-border/divider motif, and the invite/inbound/outbound dialogs all picked up the theme; name entry, settings, and manual-connect stayed on Material defaults, as scoped. `TwoDeviceMatchScreen.kt`'s `StatusScreen` was deliberately left for a follow-up, [#42](https://github.com/rustycoopes/top-trumps-game/issues/42). Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a ripple-clipping bug, a missing `role = Role.Button` accessibility regression on two `Modifier.clickable` call sites, a sub-48dp touch target on the "Play Solo" CTA, and an invite dialog's "Accept" button silently downgraded from a filled `Button` to a `TextButton`. Two physical devices were not available this session to verify the discovery/invite/tiebreak flow underneath the new visuals — that pass is still outstanding. See [slice-2-lobby-theme.md § Delivered](features/second-round-updates/WBS/slice-2-lobby-theme.md#delivered).
- 2026-08-06 — [#41](https://github.com/rustycoopes/top-trumps-game/issues/41) Slice 6 — Themed card backgrounds (card-visual-identity): a new optional `theme.backgroundImage` manifest field, resolved/validated exactly like `heroCardId` (degrades to no background at runtime, fails an all-decks CI test if malformed); `CardPalette` gained a `backgroundImage` field so every existing card-rendering call site picks it up automatically with no new parameter threading; the background renders full-bleed behind the card face at a fixed 25% alpha with a fixed-alpha scrim over the title banner/stat table, and is decoded once per deck (not per card) via Coil's own memory cache rather than a hand-rolled one. A new static drop shadow (never present mid-flip/mid-slide) was wired to exactly four surfaces: the hero card, the win-pile mini-card grid, an opened full-size pile card, and deck-picker tiles — correcting the WBS's framing along the way, since the PRD's originally-planned hero-card shadow had in fact never been implemented in any prior slice. Background art for Motorcycles and `lucys-youtubers` was hand-composed with Python/Pillow rather than AI-generated, since no image-gen service was available this session (see follow-up [#45](https://github.com/rustycoopes/top-trumps-game/issues/45)). Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a real crash risk — `CardGeometry.width`/`height` can legitimately round to `0.dp`, and Coil's `ImageRequest.size()` throws synchronously (not just failing the async load) on a non-positive dimension — plus two minor duplication nits. Flip/slide frame-rate impact and the shipped alpha constants' on-device visibility were **not** verified this session, none was available. See [slice-6-themed-card-backgrounds.md § Delivered](features/card-visual-identity/WBS/slice-6-themed-card-backgrounds.md#delivered).
- 2026-08-06 — [#45](https://github.com/rustycoopes/top-trumps-game/issues/45) Replace Pillow-placeholder deck backgrounds with real illustrated art: both `decks/motorcycles/background.webp` and `decks/lucys-youtubers/background.webp` swapped for AI-generated illustrations (pollinations.ai, `flux` model) after the free/anonymous Hugging Face FLUX space failed 4/4 attempts with ~30-minute timeouts. Pure asset swap — no manifest, source, or test change; `AllDecksThemeTest` re-confirmed passing. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found no issues. Also closed out the original slice-6 delivery's deferred on-device check: installed and drove the build on a physical device, confirming the background reads clearly at the shipped 25%-alpha/scrim composite without compromising stat-text legibility. See [slice-6-themed-card-backgrounds.md § Delivered](features/card-visual-identity/WBS/slice-6-themed-card-backgrounds.md#delivered).
- 2026-08-07 — [#47](https://github.com/rustycoopes/top-trumps-game/issues/47) Have the user flip to reveal their card before being able to play it: the player's own dealt card now shows face-down (reusing the existing deck-themed `TrumpCardBack`, with a new "Tap to reveal" caption) and only flips — reusing the opponent-reveal flip's own 420ms animation and `FLIP` sound cue — once tapped, gating stat selection and the static drop shadow on the flip having settled. `FlippableOpponentCard` generalized to `FlippableCard` (a `started: Boolean = true` parameter) so both the opponent's auto-flip and the hero card's tap-gated flip share one mechanism. No WBS slice — worked directly from the issue body per CLAUDE.md's minor-change path. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found and fixed a real UX gap — a guest reconnect mid-round would have forced an already-flipped card back face-down, needing a re-tap — using the existing `session.lastResync` signal, plus two stale-comment/doc fixes; a third finding (the action-strip prompt not accounting for the pre-flip state) was filed as [#48](https://github.com/rustycoopes/top-trumps-game/issues/48) rather than fixed here. No physical device was available to verify the flip's feel/timing on-device this session.
- 2026-08-07 — [#50](https://github.com/rustycoopes/top-trumps-game/issues/50) Slice 7 — Playing-card border & patterned back (card-visual-identity): `CardChrome` now draws a 6dp white card-stock margin around every card, front and back, all three variants — grown around the existing `CardGeometry` size rather than inset into it, so the fixed-height banner/image/table zones inside stay completely untouched. The card back's flat colour fill is replaced with a programmatically-drawn (`Canvas`, 45°-rotated tiled stripes) diagonal-stripe pattern in the deck's accent colour, plus an opaque bordered medallion holding the deck name (22sp→30sp) and caption (14sp→18sp) at visibly larger sizes. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) caught that three `solveCardWidth`-fitted `MatchScreen` call sites and three fixed-width mini-card grids (`WinPileGrid`, `CardGalleryScreen`, `DeckPickerScreen`) all needed the border's footprint growth budgeted out of their available/requested space to avoid squeezing cards on narrower screens — fixed at all six sites. Verified on a connected physical device (debug card gallery, live solo match hero/reveal cards) with no clipping; frame-rate impact and behaviour on phones narrower than the test tablet were reasoned through rather than directly measured. See [slice-7-playing-card-border.md § Delivered](features/card-visual-identity/WBS/slice-7-playing-card-border.md#delivered).
- 2026-08-07 — [#52](https://github.com/rustycoopes/top-trumps-game/issues/52) Reskin Settings to match the lobby theme (second-round-updates): `SettingsScreen.kt` picks up the same "The Pack" `LobbyPalette` visual language already shipped in `LobbyScreen.kt` — themed background/text/field/switch colours, the shared `DisplayFontFamily` title treatment, and two `LobbyScreen` composables (`LobbyTextLink`, and `PlaySoloButton` generalized into `LobbyPrimaryButton(label, onClick)`) widened to `internal` and reused for Save/Cancel/"Card gallery (debug)" instead of duplicating them. No WBS slice — worked directly from the issue body per CLAUDE.md's minor-change path. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found no bugs and no missed reuse; the only note (splitting the shared composables into their own file) was judged unnecessary for a change this size. No physical device was available to verify the restyle on-device this session.
- 2026-08-07 — [#51](https://github.com/rustycoopes/top-trumps-game/issues/51) Slice 8 — Full-screen deck backdrop (card-visual-identity): the deck's `background.webp` moved from a near-invisible per-card wash to a dominant, 60%-opacity backdrop rendered full-bleed behind the whole in-match screen (top bar, card region, action strip, win-pile grid, and an opened pile card, all states of one `InProgressScreen` composable) — the scope the art was made for. `MatchTopBar`/`MatchActionStrip` (and, per a code-review catch, `WinPileGrid`'s own header text) gained a scrim panel to hold text contrast against the now-visible image; the old per-card `CardChrome`/`TrumpCard`/`AssetTrumpCard` background-wash plumbing was deleted outright rather than layered underneath, including the prior session's uncommitted-turned-committed 0.25→0.7 alpha experiment, which this slice's scoping conversation had already flagged as moot. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) also caught a quiet `modifier`-forwarding regression introduced by the backdrop wrapper `Box`, fixed alongside the scrim gap; a stale ADR pointer was added. No physical device was available to verify the scrim's measured contrast ratio or flip/slide frame rate this session. See [slice-8-match-backdrop.md § Delivered](features/card-visual-identity/WBS/slice-8-match-backdrop.md#delivered).
- 2026-08-07 — [#53](https://github.com/rustycoopes/top-trumps-game/issues/53) Reskin match history & stats to match the lobby theme (second-round-updates): `HistoryScreen.kt` and `StatsScreen.kt` pick up the same "The Pack" `LobbyPalette` visual language already shipped in `LobbyScreen.kt` — themed background/title/link colours, bordered card-shaped match/stat rows, dashed section dividers, and dashed-border empty-state hints, reusing three `LobbyScreen` composables widened to `internal` (`LobbyTextLink`, `DashedDivider`, and `EmptyLobbyHint()` generalized into `LobbyHintBox(message)`) instead of duplicating them. No WBS slice — worked directly from the issue body per CLAUDE.md's minor-change path. Code review (run in parallel by `code-review-master` and `code-quality-guardian`) found no bugs; two non-blocking nits were fixed in-slice (a stale KDoc reference and a byte-for-byte-duplicated bordered-row modifier chain across the two new screens, consolidated into a shared `Modifier.lobbyCard()` next to `LobbyScreen`'s existing `dashedBorder`). No physical device was available to verify the restyle on-device this session.
